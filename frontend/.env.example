# Backend base URL the dashboard talks to.
# Local dev: leave blank (Vite proxy forwards /api to 127.0.0.1:8000).
# Render static build: set to the live API, e.g.
# VITE_API_BASE=https://aibuildcare-api.onrender.com
VITE_API_BASE=
